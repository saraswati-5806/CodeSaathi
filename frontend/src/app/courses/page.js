"use client";
import { useState } from "react";
import AppShell from "../../components/AppShell";
import { demoCourses } from "../../lib/demoData";
export default function CoursesPage(){const [q,setQ]=useState("");const filtered=demoCourses.filter(c=>`${c.title} ${c.category} ${c.description}`.toLowerCase().includes(q.toLowerCase()));return <AppShell><section><div className="card mb-6"><p className="text-purple-300 font-bold">Course Catalog</p><h1 className="page-title mt-2">Choose Your Learning Path</h1><p className="text-slate-300 mt-4">Demo catalog includes thumbnails, YouTube lecture URL, notes, modules, progress and completion status.</p><input className="field mt-6" placeholder="Search course..." value={q} onChange={e=>setQ(e.target.value)}/></div><div className="grid-fit">{filtered.map(c=><div key={c.id} className="card"><div className={`h-40 rounded-3xl bg-gradient-to-br ${c.thumbnail} mb-5 p-5 flex items-end`}><span className="badge bg-slate-950/80">{c.category}</span></div><h2 className="text-2xl font-bold">{c.title}</h2><p className="text-slate-400 mt-3 leading-7">{c.description}</p><div className="flex flex-wrap gap-2 mt-4"><span className="badge">{c.level}</span><span className="badge">{c.duration}</span>{c.completed&&<span className="badge">Completed</span>}</div><div className="progress mt-5"><span style={{width:`${c.progress}%`}}/></div><p className="text-sm text-slate-400 mt-2">Progress: {c.progress}%</p><div className="grid sm:grid-cols-2 gap-3 mt-5"><a className="btn-blue" href="/learning-workspace">Continue</a><a className="btn-dark" href="/certificates">Certificate</a></div></div>)}</div></section></AppShell>}
